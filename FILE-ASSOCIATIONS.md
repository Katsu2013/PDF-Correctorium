# ファイルからの起動と関連付け用アイコン

PDF Correctorium は、起動時に渡された1つの `.pdf` または `.pdfocrproj` を読み込み、先頭ページを表示します。プロジェクトでは保存済みのしおり・文書情報も復元します。拡張子の大文字／小文字は区別せず、空白や日本語を含むパスにも対応します。

元PDFを外部参照するプロジェクトでは、そのPDFが存在し、保存時の指紋と一致する必要があります。PDF内包型のプロジェクトは元の外部PDFがなくても開けます。読込に失敗した場合はエラーを表示し、画面は別のファイルを開ける状態で残ります。

## 起動コマンド

Windowsの関連付けツールやインストーラーでは、次の形の「開く」コマンドを指定します。実行ファイルとファイル引数の両方を引用符で囲みます。

```text
"C:\配置先\PdfCorrectorium.exe" "%1"
```

ファイルごとに新しいアプリケーション画面が起動します。既存画面への転送は行いません。複数のファイルを1つのコマンドにまとめて渡す形式には対応していません。

## 同梱アイコン

| 種類 | ビルドフォルダー内のアイコン |
|---|---|
| PDF | `Icons\PdfDocument.ico` |
| PDF Correctorium プロジェクト | `Icons\PdfCorrectoriumProject.ico` |

各ICOには16、24、32、48、64、128、256ピクセルの画像を含めています。関連付けの `DefaultIcon` には、対応するICOの絶対パスを指定してください。プロジェクトのアイコンには「P」の印が付いています。登録項目の仕様は [Microsoftのファイル種類の資料](https://learn.microsoft.com/en-us/windows/win32/shell/fa-file-types)を参照してください。

バックアップなどの補助アイコンも `Icons` に同梱しています。ただし、通常の拡張子ベースの関連付けでは `.bak.pdfocrproj` や `.autosave.pdfocrproj` も `.pdfocrproj` として扱われ、ファイル名の途中にある接尾辞だけではアイコンは切り替わりません。

## 関連付け設定について

このビルドはWindowsの関連付け・既定アプリ・レジストリを自動変更しません。専用ファイルアイコンの表示には、アイコン指定を含む関連付け登録が別途必要です。特にPDFの既定アプリは、Acrobatなどの現在の設定を維持しています。

関連付け後に配置先を移動・削除すると起動できなくなるため、関連付けには継続して使う固定フォルダーの実行ファイルとアイコンを指定してください。毎回作成する日時付きビルドフォルダーへ自動で関連付けを切り替える処理はありません。
