# リボンUI検討履歴・要望トレーサビリティ

## 1. この文書の位置付け

この文書は、2026年9月20日から23日に行ったリボンUI検討について、当初案、モックアップでの試行錯誤、ユーザー要望、採否、現行 v1.0.0-dev.174 との差分を時系列で整理した履歴である。

- **現行仕様の正本**: [RIBBON-UI.md](RIBBON-UI.md)
- **本書**: なぜ現行仕様になったか、何が未反映かを残す設計履歴
- **参照した会話**: Antigravity の要約ログを主資料とし、必要箇所を完全ログで照合
- **対象外**: 内部システムメッセージ、思考過程、ツール呼び出しの引数・戻り値は転載しない

参照元は次のとおりである。いずれもリポジトリ外の作業履歴であり、移動・削除される可能性がある。

- 完全ログ: `C:\Users\katsu\.gemini\antigravity\brain\910cc43a-cbe5-4946-88d2-f58e04806bd3\.system_generated\logs\transcript_full.jsonl`
- 要約ログ: `C:\Users\katsu\.gemini\antigravity\brain\910cc43a-cbe5-4946-88d2-f58e04806bd3\.system_generated\logs\transcript.jsonl`
- インタラクティブモックアップ: `C:\Users\katsu\.gemini\antigravity\brain\910cc43a-cbe5-4946-88d2-f58e04806bd3\ui_mockup.html`

## 2. 検討の全体像

検討は、単純な「ツールバーをリボンへ置換する」話ではなく、次の順で進んだ。

1. ツールバーの省スペース性と、リボンの発見しやすさを両立する案を検討した。
2. リボンと常設ツールバーの同時表示、上／下／左／右配置、クラシックUIを比較した。
3. メニューバーを残すか、リボンから表示を切り替えるか、ファイル操作の重複をどう避けるかを検討した。
4. 実アプリのベクターアイコン、キャプション位置、表示モードの選択方法、回転操作の意味を詰めた。
5. ツールバーや左右ペインの移動、ドッキング、タブ集約、フローティングをモックアップで比較した。
6. OCR編集の用語、段落／行／文字の配置、タブの密度、右プロパティとの責務分担を反復調整した。
7. 保存・PDF出力・Undo／RedoをQATへ集約し、実在するコマンドだけに絞った。
8. 最終的に、常設ツールバーとの同時表示ではなく、共通QATを残して「リボンUI」と「クラシックメニュー＋ツールバー」を排他的に切り替える実装へ収束した。
9. dev.173でリボンの視認性、コンテキスト表示、UIモード保存、キーボード操作と診断を強化した。
10. dev.174でファイル操作を左端のファイルBackstageへ分離し、ホームとの重複を解消した。

## 3. 時系列の整理

### 第1段階: 発端と併用構想

ユーザーは、従来ツールバーの「小さく、柔軟に配置しやすい」長所と、リボンの「機能を見つけやすい」長所を両立したいと提示した。ページ移動、ページ表示、ズームなどは常設ツールバーに残し、専門的な編集機能をリボンへ置く案が中心になった。

この段階では、フル／簡易／折りたたみリボン、リボン＋上部または下部の常設ツールバー、クラシックUIという複数案を比較する方針だった。

### 第2段階: モックアップと操作方式

実装前にHTMLモックアップを作り、リボン＋上部ツールバー、リボン＋下部ツールバー、クラシックUIを切り替えて比較した。表示モードと編集モードにはアイコンを付け、表示形式は単一／見開き、スクロール方式はページ切り替え／連続として検討した。

ユーザーからは、選択肢にはセグメントボタン、単純な真偽値にはON／OFFトグルを使う方が自然との指摘があった。「めくり」は意味が伝わりにくいため、「連続スクロール」の明示へ改めた。汎用絵文字ではなく、既存の `VectorIcons.xaml` と同じ意匠を使う方針も決まった。

ツールバーの文字は、アイコンの右、下、非表示の3形式をモックアップで比較した。

### 第3段階: メニューとファイル操作の重複

リボン表示中にメニューバーが必要かを検討した。初期案では、メニューを既定で隠し、リボン内またはファイルメニューから表示を戻せるようにした。ユーザーは、ホームに「ファイルを開く」がある一方でファイルメニューにも同じ操作がある重複を指摘した。

モックアップでは一度、ホームからファイル操作を外して専用ファイルメニューへ集約した。ただし最終実装では、リボンとクラシックメニューを同時表示しない方式へ変わり、ホームへPDF／プロジェクトを開く操作などを再配置している。この点は初期要望から方針が変化した箇所である。

### 第4段階: 密度、左右ツールバー、回転の意味

横1列の編集モード切り替えは間延びして見えるとの指摘から、モックアップでは4モードを2×2にした。常設ツールバーはAcrobat風の左／右配置も比較し、上／下／左／右を切り替えられる試作へ進んだ。

回転アイコンが「表示だけの回転」か「PDFページ自体の回転」か判別しにくい問題も確認した。ページ自体を変更する回転は、閲覧用常設バーではなく、ページタブやページ一覧の近くへ置き、用紙＋回転矢印のアイコンと明確なツールチップを使う案になった。

### 第5段階: 移動・ドッキングの検討

常設ツールバーを右クリックで上／下／左／右へ移動する案、ドラッグで吸着させる案、フローティングさせる案を比較した。ページ一覧／しおりとOCRプロパティについても、左右反転、片側へのタブ集約、Acrobat風の引き出し、浮動小窓をモックアップへ追加した。

これらは実現可能性の検討とモックアップ比較までで、現行実装には入れていない。

### 第6段階: OCR編集の用語とリボン密度

段落／行／文字はフルリボン内に縦3段で置く案となり、ページ一覧／しおり内のミニツールバーは対象に近い操作として残す方針になった。「段落単位」のようなシステム寄りの表現より、「段落編集」「行編集」「文字編集」の方が作業を表すとの指摘も反映した。

さらに、アプリ自体がOCR解析を実行する画面ではないため、「OCR編集」という名称は誤解を招くと指摘された。モックアップでは一度「テキスト編集」へ改称し、段落／行／文字はテキスト編集時だけ使えるようにした。

ホーム、表示・検証、テキスト・校正は、少なすぎる状態と多すぎる状態の両方を経た。詳細操作をリボンへ過剰に持ち上げた結果、ごちゃつきが生じたため、選択対象の細かな調整は右プロパティ、全体操作やモード切り替えはリボンという責務分担へ戻した。

### 第7段階: QATと実在機能への収束

ユーザーの提案により、上書き保存、PDF出力、元に戻す、やり直すをQATへ集約した。モックアップに出す操作が実在するかを再確認し、見た目だけのコマンドを置かず、既存コマンドへ接続できるものだけを実装対象にした。

ページ追加、削除、回転、しおり操作は左ペインに残し、文字列、座標、整列、文字送りなど選択対象の詳細は右プロパティを主な操作場所とした。

### 第8段階: 実機実装とdev.173での強化

`feature/ribbon-toolbar-trial` に実装した最終構成は、共通QAT、3タブのリボン、クラシックメニュー＋ツールバー、左右ペイン、ステータスバーである。リボンとクラシック上部UIは同時表示せず、QATのボタンで切り替える。

dev.173では、QATとリボンの操作面を拡大し、ホバー／選択／キーボードフォーカスを分離した。編集・校正タブは現在の編集モードに応じて内容を入れ替え、UIモードを設定形式17へ保存する。Alt+1～5、Alt+H／E／V、F6／Shift+F6、設定移行と再起動相当の復元を診断対象にした。

### 第9段階: ファイルBackstageへの分離

ホームとファイル操作の重複を解消するため、dev.174でリボンの左端に「ファイル」タブを追加した。選択時は別ウィンドウを開かず、QATを残してリボン、左右ペイン、PDFプレビュー、ステータスバーをBackstageへ置き換える。PDF／プロジェクト／OCRデータの読込、保存、別名保存、PDF出力、文書プロパティ、設定、バージョン情報、終了、最近開いたファイルを集約した。

ホームからファイル操作を撤去し、戻るまたはEscで元のリボンタブへ復帰する。PDF表示位置や選択状態は背後で保持し、クラシックUIへ切り替えた場合はBackstageを閉じる。

## 4. 要望と現行実装の対比

凡例: **採用**は現行実装へ反映、**一部採用**は目的の一部を別方式で実現、**未反映**はモックアップまたは検討のみ、**要再確認**は初期要望と最終実装が異なる項目を示す。

| 要望・論点 | 検討上の結論 | dev.173の状態 | 判定 |
|---|---|---|---|
| リボンとツールバーの長所を両立 | 専門操作はリボン、頻出操作は常設部へ | 共通QAT＋リボン／クラシックの排他切替。常設ナビゲーションはない | 一部採用 |
| ページ移動・表示・ズームを常設 | UIモードに依存しない専用ナビゲーションツールバー | リボンのホーム、クラシックツールバー、既存ステータス領域へ分散 | 未反映（重要） |
| フル／簡易／折りたたみリボン | 画面高さに応じて選択可能にする案 | 未実装 | 未反映 |
| 表示形式はセグメント、連続はON／OFF | 選択肢と真偽値で部品を使い分ける | 表示・検証ではコンボボックスを使用 | 要再確認 |
| 実アプリと同じアイコン意匠 | 既存ベクターを優先し不足分を追加 | 専用ベクターアイコンを使用 | 採用 |
| キャプションを右／下／非表示から選択 | ツールバー設定として試作 | 現行は文字表示／非表示のみ。下配置なし | 未反映 |
| リボンからメニュー表示を切り替える | メニュー表示チェックを試作 | メニュー単体の表示切替はなく、クラシックUIへ切替 | 一部採用 |
| ホームとファイルメニューの重複を避ける | ファイル操作をBackstageへ集約 | dev.174でホームから撤去し、ファイルタブへ集約 | 採用 |
| 編集モードを横一列にしない | 2×2配置を試作 | 4モードを横一列に表示 | 未反映 |
| 左／右のAcrobat風常設ツールバー | 上／下／左／右を比較 | 未実装 | 未反映 |
| 回転が表示回転かページ回転か明確にする | ページ操作の近くへ置き、専用アイコンと説明を使う | 左ペインにページ回転を残し、用途を明示 | 採用 |
| ツールバーを移動可能にする | メニュー選択、ドラッグ吸着、浮動を比較 | 未実装 | 未反映 |
| ページ一覧／しおり、OCRプロパティをドッキング | 集約、反転、引き出し、浮動を比較 | 固定左右ペイン。表示／非表示のみ | 未反映 |
| OCR編集単位を縦3段にする | 段落／行／文字を縦積み | 横並びの大ボタン | 未反映 |
| ページ一覧／しおりのツールバーを残す | 対象に近い操作は左ペインへ残す | 維持 | 採用 |
| 「単位」を避け「段落編集」等にする | 動作名を採用し、見出しを省く案 | ボタン名は採用。ただし「編集単位」見出しは残る | 一部採用 |
| 「OCR編集」を誤解の少ない名称へ変える | モックアップでは「テキスト編集」 | 現行は「OCR編集」 | 未反映 |
| 段落／行／文字は該当編集モードだけに出す | コンテキスト連動 | OCR編集時だけ表示 | 採用 |
| テキスト・校正のボタン過不足を解消 | 右プロパティとの責務分担を明確化 | モード別コンテキスト表示へ整理 | 採用 |
| 保存、PDF出力、Undo／RedoをQATへ置く | どのタブからも使える共通操作にする | QATへ常設 | 採用 |
| 実在する機能だけを置く | コマンドと画面イベントを照合 | 現行リボンは既存処理へ接続 | 採用 |
| リボンとクラシックを比較・切替 | ワンクリック切替 | QATから排他的に切替、設定へ保存 | 採用 |
| キーボード操作とアクセシビリティ | F6巡回、アクセスキー、Automation情報 | 診断を追加して維持 | 採用 |

## 5. 足りない項目・考慮不足の監査

### 5.1 仕様判断がまだ必要な項目

次は不具合とは限らないが、ユーザーの明示要望と現行実装が一致していない。現行を正式採用するか、次版でモックアップ案へ寄せるかを決める必要がある。

1. **編集モードの横一列配置**: 2×2案ではなく横一列へ戻っている。
2. **段落／行／文字の配置と見出し**: 縦3段・見出しなし案に対し、横並び・「編集単位」見出しありとなっている。
3. **「OCR編集」の名称**: 「テキスト編集」案を経たが、現行ラベルはOCR編集のままである。
4. **ページ表示の選択部品**: セグメント＋連続ON／OFF案に対し、現行はコンボボックスである。
5. **メニューバー表示切替**: リボン内の表示チェックではなく、クラシックUI全体への切替で代替している。

### 5.2 レイアウトと表示環境

1. **狭い幅への対応**: ウィンドウ最小幅は1050 DIPだが、リボングループは横方向の固定配置が中心である。日本語より長い英語ラベル、125～200% DPI、文字拡大時に、切れ、重なり、操作不能が起きないかの実画面確認が不足している。
2. **リボンのオーバーフロー方針**: グループの縮小、折りたたみ、横スクロール、優先度順の退避のいずれを採るか未定である。
3. **高コントラストとテーマ**: 一部に固定色があり、Windows高コントラスト、ダークテーマ、色覚多様性の検証は明示されていない。
4. **タッチ操作**: ボタン寸法は拡大したが、タッチ、ペン、2-in-1端末での誤操作やホバー非依存性は未評価である。

### 5.3 キーボード、読み上げ、翻訳

1. **コマンド単位のKeyTip**: QATとタブのAltキーはあるが、リボン内の各コマンドへ直接到達するKeyTip体系は未整備である。
2. **Automation名の翻訳**: QATなど一部の `AutomationProperties.Name` はXAMLに日本語で固定されている。英語表示時のスクリーンリーダー出力を実機確認する必要がある。
3. **スクリーンリーダー実機試験**: 自動診断はメタデータとフォーカス巡回を確認するが、Narrator／NVDAによる読み上げ順、状態通知、コンテキスト内容の切替通知は未確認である。
4. **IMEとアクセスキー**: 日本語IME使用中、メニュー／タブ／QATのAlt操作が衝突しないかの手動確認が必要である。

### 5.4 状態保存と障害時の挙動

1. **切替直後の終了**: UIモード切替時の保存は非同期で開始される。切替直後にアプリを終了した場合にも保存完了が保証されるか、終了処理との連携を確認する余地がある。
2. **選択タブの復元**: 現在はUIモードを保存するが、最後に開いていたリボンタブは保存しない。必要性を判断する必要がある。
3. **表示状態のリセット導線**: 将来ドッキングやリボン折りたたみを導入するなら、「標準レイアウトへ戻す」を同時に設計する必要がある。

### 5.5 情報設計と誤操作防止

1. **保存とPDF出力の違い**: QATの保存はプロジェクト保存、PDF出力は成果物生成である。初回利用者が混同しない文言、ツールチップ、未保存状態表示を確認する必要がある。
2. **破壊的操作の区別**: ページ回転、ページ削除、墨消しなど保存結果へ影響する操作は、閲覧操作と明確に分離し、Undo可否と保存影響をツールチップで統一して説明する必要がある。
3. **コンテキスト切替の気付きやすさ**: 編集モードを変えると編集・校正タブの内容が入れ替わる。タブを開いていない利用者にも変化が伝わるか、必要ならタブの自動選択または状態通知を検討する。
4. **QATのカスタマイズ**: 現在のQATは固定である。ユーザーごとの追加・削除・並べ替えを提供するかは未決であり、必須要件ではない。

### 5.6 検討資料の保全

1. **モックアップがリポジトリ外**: `ui_mockup.html` はAntigravity作業領域にしかなく、将来参照できなくなる可能性がある。保存する場合は、外部CDN依存の有無、ライセンス、日時、当時の状態を明記してリポジトリへスナップショット化する必要がある。
2. **会話ログがリポジトリ外**: 本書で要点は保全したが、原文を長期保管する場合は個人情報、絶対パス、内部メッセージ、巨大なツール出力を除去した会話本文版を別途作る方が安全である。
3. **採否の承認記録**: モックアップで提示された案と実装された案の間に差がある。上の「要再確認」6項目について、採用／見送りを明記すると将来の手戻りを防げる。

## 6. 作業領域優先での再整理

2026年9月23日のPDF-XChange Editor、Excel、dev.173比較画像により、検討の優先順位を修正する。焦点は「リボンへ何を追加するか」ではなく、**上部UIの高さと余白を減らしながら、ページ移動・ズーム・表示形式だけは常設すること**である。

### 6.1 画像と現行コードから分かったこと

- dev.173は、標準タイトルバーの下に独立QAT、タブ列、98 DIPのコマンド領域を積んでいる。
- 大ボタンは78 DIP、タブは左右各18 DIP、グループは左右各8 DIPのパディングを持つ。機能数が少ないグループにも同じ寸法を適用するため、余白が広く、試作的で間延びして見える。
- 比較画像上の概算では、dev.173のタイトルバーからリボン下端までが約205ピクセル、PDF-XChange Editorが約165ピクセル、Excelのタイトル／タブ／リボンが約155ピクセルである。厳密なDPI比較ではないが、dev.173が約40～50ピクセル高いことは視覚上の懸念と一致する。
- 現行の「表示・移動」グループはホームタブ内にあるため、編集・校正や表示・検証タブを開くと同じ位置から操作できない。
- 左ペインのページツールバーはページ追加・削除・ページ自体の回転を担当しており、ユーザーが残したい「前後ページ、ページ番号、ズーム、表示形式」の常設ツールバーとは目的が異なる。

### 6.2 再整理後の基本構造

切り替えるのは、次のコマンドシェルだけとする。

- **クラシック**: メニューバー＋編集ツールバー
- **リボン**: QATをタブ列へ統合したコンパクトリボン

切り替えずに常設するものは、次のドキュメントナビゲーションである。

- 前ページ、現在ページ／総ページ、次ページ
- ズーム縮小、倍率、ズーム拡大、幅合わせ、全体表示
- 単一／見開き
- 連続スクロールON／OFF

常設ナビゲーションは高さ30～32 DIPを目標とし、切替コマンドシェルとPDFキャンバスの間に置く。リボン側はQAT専用行をなくし、タブ列30～32 DIP、コマンド領域72～76 DIPへ圧縮する。常設バーを追加した状態でも、現行の上部UIより低くする。

### 6.3 責務の境界

| 場所 | 置く操作 | 置かない操作 |
|---|---|---|
| QAT | プロジェクト保存、PDF出力、Undo、Redo | ページ移動、詳細編集 |
| 常設ナビゲーション | ページ移動、ズーム、表示配置、連続切替 | ページ削除、ページ自体の回転 |
| リボン／クラシック編集ツールバー | モード、検索、検証、文書全体の編集 | 右プロパティの細かな入力 |
| ページ一覧ツールバー | ページ追加・削除、選択ページの回転、しおり | ビューのズームや連続表示 |
| OCRプロパティ | 選択対象の文字、座標、整列、文字送り | 文書全体のモード切替 |

この区分により、リボンとクラシックを切り替えても、閲覧位置に関わる操作は同じ場所に残る。ページ編集と表示操作の誤認も避けられる。

### 6.4 見た目の採用基準

- 現行よりPDFキャンバスの高さを減らさない。
- 大ボタンを各タブ1～2個へ限定し、補助操作は2段／3段の小型ボタンにする。
- 2×2の編集モードと縦3段の段落／行／文字を再評価する。
- 一律の余白ではなく、ExcelやPDF-XChangeのように機能密度を持たせる。
- リボンは折りたためるが、常設ナビゲーションは折りたたまない。
- 最小幅、英語、高DPI、高コントラスト、キーボードフォーカスで確認する。

詳細な寸法予算と構成案は [RIBBON-UI.md](RIBBON-UI.md) の「次期見直しの設計前提」を参照する。

## 7. 次の判断順序

次回のUI改修では、次の順で決めると変更範囲を制御しやすい。

1. 独立QAT行をなくす方法、72～76 DIPのコンパクトリボン、30～32 DIPの常設ナビゲーションを、実寸モックアップで比較する。
2. 同じウィンドウ寸法で、現行dev.173、コンパクトリボン、クラシックUIのPDFキャンバス面積を測定する。
3. 「OCR編集」の名称、2×2モード、縦3段の編集対象、表示セグメントという直接要望の採否を確定する。
4. 日本語／英語、最小幅、100／125／150／200% DPI、高コントラストでリボンの実画面を確認する。
5. Automation名の翻訳、スクリーンリーダー、コマンド単位KeyTipを補う。
6. ツールバー移動とペインドッキングは、コンパクト化とは分離した「ワークスペース可変化」機能として設計する。
7. モックアップを長期保管するなら、現状の外部ファイルをそのまま正本にせず、由来と依存関係を記した固定スナップショットとして保存する。

## 8. 現時点の結論

現行dev.173は、QAT、3タブ、モード別コンテキスト表示、左ペインツールバー、右プロパティとの責務分担、クラシック切替、設定保存、基本的なキーボード操作という中核を実装している。一方、初期要望にあったページ移動・ズーム・表示形式の常設ナビゲーションは未実装であり、現行リボンは比較対象より高く、余白も広い。次期見直しでは、常設バーを単純追加せず、QAT行の統合とリボンの圧縮を同時に行う必要がある。常設可動ツールバー、ペインドッキング、キャプション位置は、その後の別課題とする。

また、「OCR編集」の名称、編集モードと編集対象の配置、表示部品、メニュー切替は、ユーザーの明示要望と現行実装の差が残る。ホームのファイル操作はdev.174のBackstageで解消した。残る項目を曖昧なまま「すべて反映済み」と扱わず、次回の仕様判断対象として残す。
