# リボンUI／QAT／クラシックUI切り替え仕様

`feature/ribbon-toolbar-trial` の v1.0.0-dev.174 で実装している、ファイルBackstage、リボンUI、クイックアクセスツールバー（QAT）、クラシックUI切り替えの現行仕様を示す。画面には実在するコマンドだけを配置し、左ペインのページ／しおり操作と右プロパティの詳細編集は既存の場所に残す。

本書は「現在の実装仕様」を正本とする。初期のリボン＋常設ツールバー案、メニュー併用、左右ツールバー、ドッキング、モックアップの反復、採否、現行実装との差分、未決事項は [リボンUI検討履歴](ribbon_discussion_history.md) に分離して記録する。

## 1. 画面構成

ウィンドウ上部は次の順に並ぶ。

1. QAT（両UIで常時表示）
2. ファイル／ホーム／編集・校正／表示・検証から成るリボン、またはクラシックメニュー＋ツールバー
3. ページ一覧／しおり、プレビュー、OCRプロパティから成る作業領域
4. ステータスバー

QATは高さ36 DIP、リボンのコマンド領域は高さ98 DIPを基準とする。大ボタンは高さ78 DIP、小ボタンは高さ28 DIPで、グループ間には区切りと8 DIPの内側余白を設ける。ホバー、選択、無効、キーボードフォーカスはそれぞれ異なる表示にする。

ファイルタブを選ぶと、QATだけを残し、リボン、左右ペイン、プレビュー、ステータスバーをBackstageで置き換える。背後のPDF表示位置、選択、編集モードは変更しない。

## 2. QAT

| 操作 | コマンド | キー | 補足 |
|---|---|---|---|
| プロジェクトを上書き保存 | `SaveProjectCommand` | Ctrl+S / Alt+1 | フロッピーの専用アイコン |
| 編集済みPDFを別名で出力 | `ExportPdfCommand` | Alt+2 | PDF出力アイコン |
| 元に戻す | `UndoCommand` | Ctrl+Z / Alt+3 | ページ回転ではなくUndo専用アイコン |
| やり直す | `RedoCommand` | Ctrl+Y / Alt+4 | ページ回転ではなくRedo専用アイコン |
| リボン／クラシック切り替え | `ToggleRibbonUiModeCommand` | Alt+5 | 遷移先をラベルとツールチップに表示 |

アイコンのみの各ボタンには、表示言語に対応するツールチップ、`AutomationProperties.Name`、`AutomationProperties.AccessKey`を設定する。

## 3. リボン

### ファイル（Alt+F）

通常のリボンコマンド面ではなく、メインウィンドウ内のBackstageを開く。

- 開く: PDF、プロジェクト、NDLOCR-Liteデータ
- 保存と出力: 上書き保存、名前を付けて保存、PDF出力
- 管理: 文書のプロパティ、設定、バージョン情報、終了
- 最近開いたファイル: ファイル名、保存場所、フルパスを一覧表示
- 復帰: 戻るボタンまたはEsc。開く／保存／管理操作を選んだ場合も通常画面へ戻ってから処理する

Backstageは別ウィンドウを作らず、現在のウィンドウ内で作業領域を置き換える。リボンモードからクラシックモードへ切り替えた場合は自動的に閉じる。

### ホーム（Alt+H）

- モード切替: OCR編集、読み順編集、校正・確認、墨消し
- 表示・移動: 前後ページ、ページ番号、幅に合わせる、全体表示、100%
- 検索・校正: 検索、置換、コメント

### 編集・校正（Alt+E）

現在の `EditorInteractionMode` に応じて内容を排他的に切り替える。

| モード | 表示する操作 |
|---|---|
| OCR編集 | 段落／行／文字編集、領域追加・削除、OCR表示、整列、文字送り、一括調整、他ページへ反映、品質チェック |
| 読み順編集 | 1つ前へ、1つ後へ、読み順再計算、OCR表示 |
| 校正・確認 | 対象絞り込み、前／次の対象、確認して次へ、品質チェック |
| 墨消し | 指定方法、選択を墨消し、選択範囲を削除、ページ内を解除 |

読み順・校正・墨消しでOCR編集専用操作を無効表示のまま残さず、不要な密度と誤操作を避ける。右プロパティにある色、座標、文字列などの詳細入力は重複配置しない。

### 表示・検証（Alt+V）

- サイドバー表示: ページ一覧、OCRプロパティ、ステータスバー、プロパティ説明
- ページレイアウト: 単一／見開きとページ切り替え／連続スクロール
- 見開きの詳細: 表紙を単独表示、左綴じ、右綴じ（見開き時のみ有効）
- 品質・文書情報: OCR品質、文書情報
- 検証・保守: プロジェクト検証、バックアップ復旧、ログ、バージョン情報

検索、置換、品質、検証、復旧、文書情報には用途固有のベクターアイコンを使用し、拡大や回転アイコンの流用を避ける。

## 4. UIモード保存

`ApplicationSettings.UseRibbonUi` が現在のUIを保持する。設定形式は17で、切り替え直後に `ApplicationSettingsService` へ保存する。次の起動ではViewModel構築時の設定読込から復元する。

- 形式16以前または項目のないJSON: `true`（リボン）へ移行
- `false`: クラシックメニュー＋ツールバーを表示
- 設定画面、設定の移出入、同時起動時のプロパティ単位マージでも値を保持

UIモードはアプリ全体の表示設定であり、PDFプロジェクトの `formatVersion` や `minimumApplicationVersion` には保存しない。

## 5. キーボードとアクセシビリティ

- `TabItem` の `FocusVisualStyle` を消さず、テンプレートでも青い2 DIP枠を表示する。
- トグルはホバー、選択、キーボードフォーカスを別々に示す。
- F6 / Shift+F6の先頭ペインは、リボン表示中は `MainRibbonTabControl`、クラシック表示中は `MainToolbarPanel` とする。
- Backstage表示中のF6 / Shift+F6は、戻る、ファイル操作、最近開いたファイルを巡回する。EscはBackstageを閉じる。
- Ctrl+O、Ctrl+S、Ctrl+Z、Ctrl+Y、Delete、Escなど既存キーはUIモードに依存しない。
- 日本語／英語切り替え時は固定ラベル、ツールチップ、動的なUI切替ラベルを再翻訳する。

`--keyboard-test` は4つのリボンタブの名前とアクセスキー、Backstageの全面表示・日本語／英語・Automation名・復帰・F6巡回、QATの名前・キー・ツールチップ、各編集モードのコンテキスト操作を検証する。`--settings-test` は形式移行、クラシックUIの保存、切り替え後の再起動相当の復元を検証する。

## 6. クラシックUIとの役割分担

クラシックUIは削除せず、メニュー階層と従来ツールバーをそのまま利用できる。QAT、中央作業領域、左右ペイン、ステータスバーは共通である。ページ追加・削除・回転としおり操作は対象を見ながら使うため左ペインに残し、リボンへ重複させない。

## 7. 次期見直しの設計前提（未実装）

添付されたPDF-XChange Editor、Excel、dev.173の比較画像を基に、次回は「リボンを増築する」のではなく、**PDF作業領域を最大化するために上部UI全体を再構成する**。この節は現行仕様ではなく、次期設計の制約である。

### 7.1 切替対象と常設対象を分離する

上部UIは次の2層に分ける。

1. **切替コマンドシェル**: 「クラシックのメニュー＋編集ツールバー」または「コンパクトリボン」のどちらか一方を表示する。
2. **常設ドキュメントナビゲーション**: 前ページ、ページ番号／総ページ、次ページ、ズーム、幅合わせ、全体表示、単一／見開き、ページ切り替え／連続スクロールを、両UIモードで同じ位置に残す。

常設ナビゲーションは、ページ一覧内の「ページ追加・削除・ページ自体の回転」ツールバーとは別物である。前者は閲覧位置と表示方法、後者はPDF構造の編集を担当する。ページ自体の回転を常設ナビゲーションへ混在させない。

```text
リボン表示
┌ QAT＋タブ ───────────────────────── UI切替 ┐  30～32 DIP
├ コンパクトなリボンコマンド領域 ─────────────┤  72～76 DIP
├ 前 | 現在/総数 | 次 | − 100% ＋ | 幅 | 全体 | 単一/見開き | 連続 ┤  30～32 DIP
└ PDF作業領域

クラシック表示
┌ メニューバー ─────────────────────────────┐  24～26 DIP
├ クラシック編集ツールバー ──────────────────┤  30～32 DIP
├ 前 | 現在/総数 | 次 | − 100% ＋ | 幅 | 全体 | 単一/見開き | 連続 ┤  30～32 DIP
└ PDF作業領域
```

QATは独立した36 DIPの行を維持せず、リボン表示ではタブ列へ、クラシック表示では編集ツールバーへ統合する。アプリ名の重複表示も除く。これにより、常設ナビゲーションを追加しても、現行より上部全体を低くできる。

### 7.2 高さと密度の予算

現行は、QAT 36 DIP、タブ列約34 DIP、コマンド領域98 DIPが積み重なり、標準タイトルバーを除いても約170 DIPを使用する。比較画像上でも、dev.173のタイトルバーからリボン下端までは約205ピクセルで、PDF-XChange Editorの約165ピクセル、Excelのタイトル／タブ／リボン約155ピクセルより40～50ピクセル程度高く見える。これは同一画像上の概算でありDPIの厳密な測定ではないが、相対的な過大さを判断する材料になる。

次期案では、次を目標値とする。

| 状態 | タイトルバーを除く上部UIの目標 |
|---|---:|
| リボン展開＋常設ナビゲーション | 132～140 DIP以下 |
| リボン折りたたみ＋常設ナビゲーション | 62～66 DIP以下 |
| クラシック＋常設ナビゲーション | 84～90 DIP以下 |

画面の高さだけでなく、PDFキャンバスの実効面積を診断値として計測する。最小ウィンドウ、1920×1080、125／150／200% DPI、日本語／英語で比較する。

### 7.3 リボンを不格好にしない密度規則

- コマンド領域を現行98 DIPから72～76 DIPへ縮める。
- 大ボタンは各タブの主操作1～2個に限定し、現行78 DIPから60～64 DIP程度へ縮める。
- 補助操作は高さ22～24 DIPの2段または3段スタック、ドロップダウン、右プロパティへ配置する。
- タブの左右パディングは現行18 DIPずつから10～12 DIPずつへ、グループ左右パディングは現行8 DIPずつから4～6 DIPずつへ見直す。
- アイコンと文字の周囲へ一律に大きな余白を置かず、機能の重要度に応じて大型／小型を使い分ける。
- 同じグループ内でボタン高、アイコン基線、キャプション基線をそろえ、区切り線だけで間延びを作らない。
- ホームの4編集モードは横一列の大型ボタンを避け、2×2または小型2段を比較する。
- 段落／行／文字は縦3段の小型選択として再評価する。
- 右プロパティにある詳細操作をリボンへ重複させない。
- 使わないときはコマンド領域だけを折りたためるようにし、常設ナビゲーションは残す。

### 7.4 常設ナビゲーションの最小構成

常設するのは、文書を読み進める間にタブを問わず使う操作だけとする。

| グループ | 常設操作 |
|---|---|
| ページ | 前、現在ページ入力、総ページ表示、次 |
| ズーム | 縮小、倍率表示／入力、拡大、幅に合わせる、ページ全体 |
| 配置 | 単一／見開きのセグメント選択 |
| 流れ | 連続スクロールON／OFF |

保存、PDF出力、Undo／RedoはQAT、ページ追加・削除・ページ自体の回転はページ一覧、OCR領域の詳細編集は右プロパティに残す。これにより、ナビゲーションバーを常設しても機能の重複と高さ増加を抑える。

### 7.5 採否判定

次回の実装判断では、単にボタンが収まることを合格条件にしない。PDF-XChange Editor、Excel、クラシックUIと並べ、次を満たす場合に採用する。

- 同じウィンドウ高でPDFキャンバスが現行dev.173より広い。
- リボン展開時でも常設ナビゲーションへ1操作で到達できる。
- リボンとクラシックを切り替えても、ページ番号、ズーム、表示形式の位置が動かない。
- 日本語／英語と高DPIで切れや重なりがない。
- 余白を減らしてもキーボードフォーカス、タッチ対象、誤操作防止を損なわない。
