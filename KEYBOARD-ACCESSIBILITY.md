# キーボード操作（dev.128）

## 割り当ての基準

表示順にA、B、Cと振る方式は使用しません。英語の操作名・慣用名を優先し、同じ画面・同じメニュー内で重なる場合は、その単語の別の文字を使います。日本語・英語で同じキーを維持し、保存などの主要操作を優先します。

| 場所・操作 | キー | 由来 |
|---|---|---|
| ファイル → PDFを開く | O | Open |
| ファイル → プロジェクトを開く | P | Project（PDFのOpenと区別） |
| ファイル → 最近開いたファイル | R | Recent（Alt+F → R、矢印とEnterで選択） |
| ファイル → 上書き保存／別名保存 | S／A | Save／Save As |
| ファイル → PDF出力／終了 | E／X | Export／Exit |
| 編集 → 元に戻す／やり直す | U／R | Undo／Redo |
| 設定 → 保存／既定値に戻す | Alt+S／Alt+D | Save／Defaults |
| 設定 → 管理 → 表示件数／履歴クリア | Alt+C／Alt+H | Count／History |
| 設定のタブ | V／E／K／R／G | View／Edit／Keyboard shortcuts／Recovery／ManaGe |
| 文書プロパティ → 適用 | Alt+A | Apply |
| 文書プロパティのタブ | D／S／F／C／V | Description／Security／Fonts／Custom／Advanced |
| 文書プロパティ → タイトル／作成者／言語 | T／U／L | Title／Author（Applyと区別）／Language |
| 検索や候補確認 → 前へ／次へ | P／N | Previous／Next |
| ページ操作 → 挿入／削除／左回転／右回転 | I／D／L／R | Insert／Delete／Left／Right |

メニュー内のキーは、そのメニューを開いた状態で使用します。たとえばPDFを開く操作は **Alt+F → O**、上書き保存は **Alt+F → S**、別名保存は **Alt+F → A** です。ウィンドウ全体のAlt+Oなどに置き換えるものではありません。従来のCtrl+O、Ctrl+S、Ctrl+Shift+Sも維持します。

文字数の多い画面でも、意味のない数字で空きを埋めません。設定画面のハンドルの太さ・不透明度・枠線色は、対応する色や大きさの入力欄へアクセスキーで移動してからTabを使用します。メイン画面のページ／しおり一覧はタブ見出し・F6から移動し、倍率スライダーは倍率欄のAlt+ZからShift+Tabで移動できます。数字1は「100%で表示」のように数値そのものに意味がある場合に限ります。

今後、項目を追加・変更する際も、英語名からの由来、主要操作との競合、日英の一致を確認し、意味に基づく割り当てを回帰検証に追加してください。

## アクセスキーとフォーカス移動

- 項目名の末尾の `(L)` などはアクセスキーです。Altを押しながらその英数字を押すと、入力欄へ移動したり、ボタン・チェック項目・タブを操作したりできます。Windowsの設定によっては下線がAltを押したときだけ表示されます。
- 入力欄の見出しは対応する入力欄を直接参照します。アイコンや一覧など、見出しを置かない部品ではツールチップに `Alt+英数字` を表示します。
- Tabで次の操作部品へ、Shift+Tabで前の部品へ移動します。ダイアログの下部にある操作ボタンは入力・選択欄の後に移動します。読み取り専用の説明値はTabの対象外ですが、マウスによる文字選択・コピーは維持します。結果一覧は読み取り専用でもキーボードで選択できます。
- タブ見出しのアクセスキーでタブを選択できます。Tabでタブ内の操作へ移動し、Ctrl+Tab／Ctrl+Shift+Tabで前後のタブへ移動できます。一覧・ツールバー・ラジオ項目の内部ではWPF標準の矢印キー操作も利用します。
- メイン画面のF6／Shift+F6は、ツールバー → ページ・しおり → プレビュー → OCRプロパティ → 倍率欄を順方向／逆方向に移動します。非表示・無効な領域は飛ばします。
- メイン画面の主要入力欄はアクセスキー、コマンドはメニューのアクセスキーまたはTab・矢印キーから操作できます。すべてのアイコンへ独立したAlt＋1文字を割り当てる方式ではありません。同じ画面でキーを重複させないため、メニューと領域移動を組み合わせます。
- OK・キャンセルにはアクセスキーを追加しません。各画面で設定済みのEnter（既定のボタン）・Esc（キャンセル）を維持します。「保存」「適用」「検索」などの操作ボタンにはアクセスキーがあります。

対象はメイン画面、文書プロパティ、設定、検索・置換、品質分析、一括文字調整、定型領域の反映設定・候補確認、画像最適化・プレビュー、進捗画面です。OS標準のファイル選択・メッセージダイアログはWindows標準のキー操作を使用します。

アクセスキーは日本語・英語の切り替えで変えません。異なるダイアログや、同時に表示されないタブの入力欄では同じキーを再利用します。未読込・処理中などで無効な操作は、アクセスキーでも実行できません。

## 変更可能なショートカットとツールチップ

「設定 → ショートカット」で次の8操作を変更できます。アクセスキーは項目に移動するための固定キー、こちらは編集コマンドを実行するための変更可能なキーです。

| 操作 | 既定の割り当て |
|---|---|
| 前の文字へ移動 | Alt+Left |
| 次の文字へ移動 | Alt+Right |
| 選択文字の送り幅を小さくする | Alt+Down |
| 選択文字の送り幅を大きくする | Alt+Up |
| 画像から文字幅を自動調整 | Ctrl+Shift+A |
| 選択文字以降を自動調整 | Ctrl+Shift+Right |
| 文字幅を等分 | Ctrl+Shift+E |
| OCR取込時の文字幅へ戻す | Ctrl+Shift+R |

8操作のボタンは現在の設定をツールチップへ表示します。設定の保存後に表示を更新し、解除した場合は「割り当てなし」（英語ではUnassigned）と表示します。行・文字の両モードに共通するボタンも同じ割り当てを表示します。文字移動・送り幅の増減は文字編集時の専用ツールバーにまとめています。既存の自動調整・等分・復元のコンテキストメニューにも設定中のキーを表示します。

## 競合と標準キーの保護

- Alt＋英数字はアクセスキー用です。Tab・Esc・Enter、F6／Shift+F6、Alt+F4、コピー・貼り付け・検索・保存などの予約済みキーを新たな編集ショートカットに設定することはできません。
- ショートカット入力欄でもTab／Shift+Tab・Esc・Enter・アクセスキーを利用でき、入力欄から移動できなくなることを防ぎます。Backspace／Deleteは割り当ての解除に使用します。
- 旧設定に予約キーとの競合がある場合、割り当てを勝手に書き換えず、該当コマンドを実行しません。ツールチップへ競合を表示するので、設定画面で別のキーへ変更してください。
- ドロップダウンにフォーカスがある場合、Alt+Down／Alt+Upはその開閉に使用します。文字幅編集の同名キーを優先して奪うことはありません。
- dev.127から、設定全体に含めたショートカットのインポート・エクスポートと、固定パネルの配置プリセットを利用できます。ショートカット専用の名前付きプリセットや、1操作への複数割り当ては未実装です。
- 「管理」タブはAlt+G（ManaGe）、取り込みはAlt+I、書き出しはAlt+X、プリセット選択はAlt+P、名前はAlt+N、登録・更新はAlt+U、選択を適用はAlt+A、削除はAlt+L（deLete）です。保存先の展開はAlt+F。設定の保存は従来どおりAlt+Sです。詳細は[操作ガイド](SETTINGS-WORKSPACES.md)を参照してください。

## 検証

`--keyboard-test <新しい出力フォルダー>`で画面・入力の回帰検証を実行できます。日本語／英語、タブごとのアクセスキー重複、Labelの移動先、OK／キャンセルの除外、ネイティブWPFのアクセスキー処理、Tab順序、予約キー、可変ツールチップと解除表示を検査します。設定・PDFは専用の検証フォルダーに保存し、利用者の設定を変更しません。キーの動作確認には一時的な画面外ウィンドウを使用し、システム全体へのキー送信は行いません。

この検証はスクリーンリーダーやIMEとの全組み合わせ、全ダイアログの端から端までのキー操作を保証するものではありません。配布前の結果は[テスト戦略](outputs/PdfCorrectorium-Documentation/docs/11_Test/11-01_TestStrategy.md)を参照してください。
