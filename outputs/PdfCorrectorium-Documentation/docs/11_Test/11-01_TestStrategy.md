# 11-01 テスト戦略

## dev.124の検証（2026-08-30）

版番号管理の修正後、リポジトリ直下のReleaseビルドは警告0・エラー0で成功した。配布フォルダー`PdfCorrectorium-v1.0.0-dev.124-win-x64-20260830-183553`の実行ファイルを使用し、次を確認した。

| 検証 | 成功件数 | 追加・確認した内容 |
|---|---:|---|
| 契約テスト | 16 | 全アセンブリの製品・数値版と保存manifestの一致、形式1.1・最小対応版dev.123の維持 |
| `tools/TestVersioning.ps1 -PublishDirectory <配布先>` | 18 | 版の上書き拒否、誤ラベル拒否、リビジョン巻戻し・更新忘れの検出、同一ソース再ビルドの許可、実バイナリーとWindowsマニフェストの一致 |
| `--document-ui-test` | 139 | タイトルバーの開発版表示、バージョン情報の製品版・4桁数値版、従来のUI検証 |
| `--persistence-test` | 38 | 保存・復旧・PDF出力の回帰検証 |
| `--review-mode-test` | 69 | 校正・確認操作の回帰検証 |
| `--file-launch-tests` | 67 | 新規プロセス13回のファイル起動検証 |

合計347件が成功し、別途`--smoke-test`も終了コード0で成功した。`build-info.json`の版番号は`1.0.0-dev.124` / `1.0.0.124`、SDKは10.0.400。発行後のソース指紋も記録値と一致した。Git情報は利用できずnullで記録している。

結果は`outputs/.verification/final-dev124-*`と`outputs/.verification/versioning-20260830-183625138`へ保存した。版管理の検証は既存成果物を変更せず、独立したテスト用履歴で拒否動作を確認する。継続運用は[版管理ルール](../../../../VERSIONING.md)に従う。以下はdev.123以前の履歴である。

## dev.123の検証

通常のリポジトリ直下からReleaseビルドを行い、警告0・エラー0。常設の保存・復旧診断 `--persistence-test` を追加した。旧スモークテストの設定形式不一致も修正した。

2026-08-30の最終検証では、日時付き配布物`PdfCorrectorium-v1.0.0-dev.123-win-x64-20260830-180424`で下記の診断を実行した。契約テストは同じソースのReleaseビルドで実行し、合計325件が成功した。

| 検証 | 成功件数 | 結果 |
|---|---:|---|
| 契約テスト | 15 | 形式1.0互換、形式拒否、意図的な空文字を含む |
| `--persistence-test` | 38 | 保存確認・失敗時保持、属性保持、空文字のPDF出力、校正制限、自動保存・復旧 |
| `--review-mode-test` | 69 | 校正・確認の操作と保存 |
| `--document-ui-test` | 136 | メニュー制限、倍率同期、UI状態 |
| `--file-launch-tests` | 67 | 新規プロセス13回でファイル起動を検証 |

上記とは別に`--smoke-test`も終了コード0で成功した。診断ログは`outputs/.verification/final-dev123-*`、起動ログは当該配布フォルダーの`logs/startup-20260830.log`に保存した。SVG図版5点は画像へ描画して目視確認済み。これらは対象を絞った回帰確認であり、Version 1.0の全受入条件の達成を意味しない。

文書の最終確認ではMarkdown 28ファイル内のローカルリンク66件、コードフェンスの対応、PowerShell例の構文、SVG 5ファイルのXML構文に問題がないことを確認した。既存の設計書PDFは2026-08-09時点の履歴資料として維持し、今回の追記・図版更新はMarkdownとSVGへ反映した。

`--persistence-test`は下記と同じ実行方法で、新規出力フォルダーを指定する。文書切替の保存確認、取消・失敗時の保持、空文字の保存と実際のPDF文字抽出、既存領域属性、品質分析の制限、全置換後の状態、操作停止後の保存、未保存プロジェクトのPDF内包復旧、内包型再保存を検証する。形式1.0互換読込と未対応形式拒否は契約テストへ追加した。

## 修正前の監査実績（2026-08-30 / dev.122）

同日の既存検証ではReleaseソリューションビルドが警告0・エラー0で成功した。追加監査では配布ビルド`20260830-143634`の診断モードと契約テストを再実行し、次の285件が成功した。今回の文書追記に伴う再ビルド・再実行の結果ではない。

| 検証 | 成功件数 | 主な対象 |
|---|---:|---|
| 依存追加なしの契約テスト | 13 | Core、ProjectFormat、Infrastructure等の契約 |
| `--document-ui-test` | 136 | 未読込時のメニュー・ショートカット制限、読込/失敗時の状態、ページ・倍率制限、単色スライダー、中央100%の変換、倍率表示同期、フラットボタン、ツールバー寸法 |
| `--file-launch-tests` | 67 | 新規プロセス13回でPDF/プロジェクト起動、空白・日本語・相対パス・拡張子大小、内包/外部参照、エラー、元ファイル不変、ICO解像度 |
| `--review-mode-test` | 69 | 状態フィルター、削除除外、読み順、前後ページ移動、確認、文字修正、Undo/Redo、保存再読込、中止、通常の配置操作制限、日英表示名と選択保持 |

`--smoke-test`は設定形式の期待値10と現行11の不一致で終了コード-1となり、この監査時点では失敗していた（dev.123で修正）。285件の成功にスモークテストを含めてはならない。また、独立した合成PDF/プロジェクトの再現確認で下記5件の不具合が再現した。監査時点の成功件数は保存の完全性や全モード経路の安全性を保証しない。

Appの診断モードは契約テストとは別に実行する。通常のソリューションビルドだけでは実行されず、統一テストランナーによる全診断の自動実行は未整備である。ViewModel→保存モデルの全属性往復、主要PDF出力、ページ構造操作、UIワークフローの網羅は未完了。当時の契約テスト13件にはFR要求IDが未適用だった。dev.123で追加した2件にはFR識別子を含めたが、全件への展開は未完了。

### 実行方法

互換SDKの選択とビルド手順は[リポジトリREADME](../../../../README.md#build)を参照する。契約テストはリポジトリ直下で`dotnet run --project tests/PdfCorrectorium.ContractTests`を実行する。診断モードはビルド済みの実行ファイルへ次のように渡し、モードごとに新しい出力フォルダーを使う。

```powershell
$diagnosticExe = Join-Path $PWD 'src/PdfCorrectorium.App/bin/Release/net8.0-windows7.0/PdfCorrectorium.exe'
foreach ($diagnosticMode in @('--document-ui-test', '--file-launch-tests', '--review-mode-test', '--persistence-test')) {
    $diagnosticName = $diagnosticMode.TrimStart('-')
    $diagnosticOutput = Join-Path $PWD ('outputs/.verification/' + $diagnosticName + '-' + (Get-Date -Format 'yyyyMMdd-HHmmssfff'))
    $diagnosticProcess = Start-Process -FilePath $diagnosticExe -ArgumentList @($diagnosticMode, ('"' + $diagnosticOutput + '"')) -WindowStyle Hidden -Wait -PassThru
    if ($diagnosticProcess.ExitCode -ne 0) {
        throw "$diagnosticMode failed: $($diagnosticProcess.ExitCode). Output: $diagnosticOutput"
    }
}
```

配布物を検証する場合は`$diagnosticExe`だけを対象の日時付きビルド内の実行ファイルへ変更する。診断は合成データと結果ファイルを出力し、失敗時は非ゼロ終了する。実際のWindows関連付け登録は変更しない。通常起動、実文書全種類、外部OCR連携の保証とは区別する。

### 未検出だった不具合と常設化した回帰テスト

以下は2026-08-30の独立した再現確認で観測したものであり、dev.123で製品修正と常設テストへの追加を行った。表の現象は修正前の記録である。

| 再現条件 | dev.122の結果（修正前） | 必要な受入・回帰確認 |
|---|---|---|
| OCR編集後、同じ画面で別PDF/プロジェクトを開く | 未保存確認なしに編集とUndoが消える | 保留入力の確定、保存/破棄/キャンセル、保存取消/失敗、読込失敗でも元文書と編集を保持すること |
| `ABCDEF`を空文字にし保存・再読込 | `ABCDEF`が復活する | 未編集と意図的な空文字を区別し、互換読込、再保存、PDF出力後の文字抽出まで確認すること |
| 親ID・均等配置・無効な出力フラグを持つ既存領域を再保存 | 親IDがnull、補正方式がAutomatic、4出力フラグがtrueになる | 未編集属性、方向情報、未表示ページを含む全属性の往復を確認すること |
| 校正モードでOCR品質分析のキーワード幅補正を実行 | 配置編集不可でも幅720→1080に変わる | 分析・候補表示を維持し、補正はUIと実行処理の両方で制限すること |
| `ABC`→`XYZ`の全置換後、既定の校正一覧を確認 | Modifiedとなり対象一覧から外れる | FR-700のNeedsReviewになり一覧に残ること、Undo/Redoと保存後も状態が一致すること |

分析画面には補正の実行確認があるため、4件目は「確認なしで変更される」という問題ではない。レビューの通常文字修正がModifiedになる仕様と、一括置換に要求されるNeedsReviewは区別して検証する。

以下のテスト層と品質ゲートは目標戦略であり、現時点の全項目合格を表すものではない。既知問題の一覧は[実装状況](../../../../IMPLEMENTATION_STATUS.md#known-implementation-defects)を参照する。

## 1. テスト層

- 単体: 座標、行列、差分、状態遷移、フォント判定、移行
- プロパティベース: 回転/逆変換、分割結合、シリアライズ往復
- 統合: PDF解析・保存、OCRインポート、外部プロセス、プロジェクトZIP
- ゴールデン: 代表PDFの描画画像、抽出文字、読み順、検証結果
- UI: 主要ワークフロー、キーボード、ドッキング復元、言語切替
- 耐障害: 保存中終了、ディスク不足、破損JSON、元PDF移動、API失敗
- 性能: 大規模書籍、遅延描画、メモリ、保存時間
- ライセンス/供給網: 依存一覧、SBOM、NOTICE、既知脆弱性

## 2. 代表データセット

- 横書き、縦書き、同一ページ混在
- 右開き/左開き、表紙有無
- 斜め行、90度見出し、ページ回転
- ルビ、縦中横、旧字体、異体字、欧文混在
- `1/7`等の誤認識、行長の過不足、文字間隔ずれ
- NDLOCR-Lite結果、Google Vision結果、PDFのみ
- サブセットフォント、ToUnicode不足、複数Content Stream
- 署名、暗号化、PDF/A、Tagged PDF、破損PDF

テストPDFは再配布権を確認し、可能なら合成データを生成する。

## 3. 保存受入条件

- 出力を複数エンジンで開ける。
- ページ数、Box、回転、画像外観が意図せず変わらない。
- 編集後文字が検索・コピーでき、旧文字や重複が残らない。
- 不可視文字は通常描画で見えず、可視化時に位置が合う。
- 読み順、ルビ属性、検索/コピー/読み上げ対象がモデルと一致する。
- 重大検証失敗時、元ファイルと既存成果物が不変。

## 4. 要求トレーサビリティ

テスト名に要求IDを含める（例 `FR_405_RotatedVerticalLine_SaveRoundTrip`）。各リリースで要求→テスト→結果を追跡する。

## 5. 品質ゲート

- リポジトリ直下の標準コマンドでReleaseビルド成功、警告方針を満たす
- 単体/統合/ゴールデン合格
- 重大なデータ損失・セキュリティ欠陥なし
- ライセンス・NOTICE・SBOM更新
- Portableクリーン環境で起動
- 日本語/英語の主要フロー確認
- 保存失敗・復旧シナリオ確認
