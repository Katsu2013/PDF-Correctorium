# 13-02 追加機能の検討一覧

受付日：2026-09-01。対象の現行版：`1.0.0-dev.126`。

以下は利用者から挙がった将来の追加要望であり、実装済み機能ではない。リリース時期・優先順位・採用する詳細仕様は未定とし、既存のVersion 1.0必須要件を置き換えない。「要望」は受付内容、「設計・受入観点」は実装前に具体化する案として区別する。この追記ではアプリ、プロジェクト形式、版番号を変更しない。

| 管理ID | 追加要望 | 状態 | 既存機能との区別 |
|---|---|---|---|
| FUT-001 | 色を選べる墨消し。選択した文字・領域を背景画像からも消す | 未実装・設計前 | OCR透明テキストの削除や色付きオーバーレイ表示だけでは満たさない |
| FUT-002 | 画面上の文字・領域をクリックしてページへジャンプ | 未実装・範囲確認待ち | しおり・ページ一覧・校正対象の前後移動とは別の操作 |
| FUT-003 | 透明テキストの不透明化、および不透明テキストの透明化 | 双方向の編集機能として未実装 | 編集用オーバーレイの表示色・不透明度とは別。FR-400の通常PDFテキスト不可視化要求を拡張する候補 |
| FUT-004 | 編集画面のプレビュータイプを選択（見開き・スクロールなど） | 未実装・設計前 | 文書プロパティでPDFに保存する初期表示設定とは別 |

## FUT-001 色指定と背景画像を含む墨消し

### 要望

- 選択した文字、または指定した領域を墨消し対象にできる。
- 墨消しに使う色を選択できる。
- OCR文字情報だけでなく、背景画像に写っている対象箇所も消せる。

### 設計・受入観点（案）

- 「範囲の指定・見た目の確認」と「墨消し済みPDFの確定出力」を分ける。色付き図形を上に置く処理だけを墨消し完了とは扱わない。
- 出力PDFでは対象の文字情報と画像内容を除去・置換し、文字抽出、画像抽出、描画で対象内容が残っていないことを検査する。重なった文字、画像の再利用、マスク、回転・拡大縮小、ページ外や非表示の関連データも検証対象として具体化する。
- 選択文字から消去範囲を作る際は、背景画像上の実際の印字範囲とのずれを確認できるようにする。OCR枠のずれで消し残しや隣接文字の巻き込みが起きる場合を試験する。
- 出力前に対象範囲・色・影響を確認し、安全な除去を検証できない場合は成功扱いで出力しない。未対応のPDF構造は理由を表示する。
- 元PDFを保持した編集プロジェクトと、配布用の墨消し済みPDFを区別する。元PDFを内包するプロジェクト、自動保存、バックアップ、作業キャッシュに機密情報が残り得ることを案内し、それらを自動的に墨消し済みとは扱わない。
- 編集途中の取消・Undoと、確定出力されたPDFから元情報を復元できないことを両立する設計にする。出力は別名保存を基本とし、元ファイルや既存のバックアップを勝手に削除しない。

### 未確定事項

文字単位・矩形以外の選択形状、範囲の余白、既定色、複数ページ一括処理、関連する注釈・代替文字列・添付物等の除去範囲と対応不能時の扱い。文書全体の機密情報自動検出・完全な無害化は、この要望だけで追加を決定したものではない。

## FUT-002 文字・領域クリックによるページジャンプ

### 要望

画面上の文字や領域をクリックすることで、目的のページへ移動できるようにする。

### 設計・受入観点（案）

- 通常のOCR選択・文字編集・領域移動と、ページジャンプの操作を区別する。通常クリックをすべてジャンプへ変更せず、モード、修飾キー、明示コマンド等の方式を検討する。
- 移動先を確認できる表示、キーボードでの実行、移動前の位置へ戻る操作を検討する。無効・削除済みの移動先は理由を表示する。
- リンクを新規設定・保存する場合は、ページ挿入・削除・並べ替え後も移動先を追跡できる識別方法と、PDF出力後の動作を設計する。
- 編集中の文字列を確定してから移動し、ページ切替によって保留入力や選択状態を不用意に失わないことを試験する。

### 未確定事項

既存PDF内のリンクを利用したいのか、任意の文字・領域へ新しい移動先を設定したいのか、両方かを確認する。本文・目次のページ番号を自動認識してジャンプする機能も別途確認が必要。同一PDF内を初期の検討範囲とする案であり、外部ファイル・Webへのリンクは未決定。設定をプロジェクト内だけに保持するか、出力PDFにも含めるかも未決定。

## FUT-003 テキストの透明・不透明の相互変換

### 要望

透明テキストを不透明な文字として表示できるようにし、反対に不透明テキストも透明化できるようにする。

### 設計・受入観点（案）

- プレビューの補助表示ではなく、PDFの文字の表示状態を編集対象とし、プロジェクトへの保存・再読込・PDF出力へ反映する。
- 文字列、位置、読み順等を保持し、見た目の透明化を文字削除や検索・コピー対象からの除外と同一視しない。FUT-001の墨消しとは独立した操作にする。
- 不透明化時の色・フォント・重なり順を検討する。背景画像にも同じ文字がある場合の二重表示を確認できるようにする。この操作だけで背景画像の文字を自動的に消すことは決定しない。
- 可視／不可視の切り替え、変更前後の確認、Undo/Redo、再出力後の描画と文字抽出を試験する。未対応の描画方式は、変換できたように見せず明示する。

### 未確定事項

文字・行・領域・ページのどの単位まで一括変更するか、部分的な不透明度も許可するか、既存の非OCRテキストまで扱う範囲、フォントを再現できない場合の扱い。画像に焼き込まれた文字そのものの透明化は別の画像処理であり、PDFテキストの切り替えと混同しない。

## FUT-004 編集プレビューの表示方式選択

### 要望

編集画面のプレビューを、見開き、スクロール等から選べるようにする。

### 設計・受入観点（案）

- 候補は「単一ページ」「単一ページの連続スクロール」「見開き」「見開きの連続スクロール」。正式な選択肢と既定値は実装前に確定する。
- アプリ内の編集表示と、PDFの初期表示設定を分離する。プレビュー方式の変更だけで出力PDFの文書設定を勝手に変更しない。
- 見開きでの綴じ方向・表紙単独表示、連続表示での現在ページの決め方、ページ番号・しおり・検索／校正ジャンプとの同期を設計する。
- 複数ページを表示しても、OCR枠、選択、編集ハンドル、クリック位置、倍率がそれぞれ正しいページ座標へ対応することを検査する。FUT-002のクリック位置判定とも共通化を検討する。
- 表示方式の切り替えで編集内容を失わず、可能な範囲で表示中の位置と倍率を保つ。大量ページでは可視範囲中心の描画・キャッシュを検討する。

### 未確定事項

連続スクロールの方向、異なるページサイズ・回転の配置、設定の保存単位（アプリ／文書／プロジェクト）、見開きをまたぐ選択範囲、既存のPDF初期表示設定を初回だけ参照するかどうか。

## 実装へ進む前の確認

- 上記の未確定事項と実装順を利用者と合意してから、既存のFR要求との対応・画面設計・保存形式・テストを具体化する。記載順は優先順位を表さない。
- 新しいUIにも意味に基づくアクセスキー、Tab順序、フォーカス表示、ツールチップを適用する。
- 現行図版に未実装の部品を混ぜない。具体的な画面構成が決まった段階で、将来案と明記した図版を追加する。
- 関連資料：[スコープ](../00_Project/00-03_ScopeAndRoadmap.md)、[機能要求](../01_Requirements/01-01_FunctionalRequirements.md)、[ロードマップ](13-01_Roadmap.md)、[実装状況](../../../../IMPLEMENTATION_STATUS.md)。
