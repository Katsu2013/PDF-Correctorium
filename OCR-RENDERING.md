# OCR描画の軽量化（dev.141）

## 対応内容

OCR領域内の文字を`OcrCharacterLayer`へまとめて描画する。従来の文字ごとの`Border`、`Viewbox`、`TextBlock`と表示コンテナーを作成しない。行全体の文字列へ置き換える方式ではなく、Unicodeテキスト要素ごとの位置・送り幅で描画するため、文字幅編集と縦書きを保持する。

- 文字の字体・描画寸法、1単位の内側余白、表示色を維持する。選択、ロック、選択＋ロック、検索一致の順に強調を上書きする。検索一致は赤色・太字のままとする。
- 描画レイヤーは入力を受け取らず、従来の領域選択、文字ヒットテスト、移動・サイズ変更・回転を使用する。読み順番号は引き続き独立した最前面レイヤーへ表示する。
- フォント描画情報は1領域あたり最大128件まで保持する。フォント・色・言語・DPI変更時に破棄する。文書や画面を静的なキャッシュへ保持しない。
- 文字分割結果とセル配列は現在の状態だけを再利用する。文字列、寸法、選択、ロック、検索強調の更新時に再生成する。キャッシュをプロジェクトやUndoへ保存しない。
- 領域サイズ変更用の8個のハンドルは、行／段落編集で選択中の場合だけ生成する。選択解除、文字編集、校正・確認モードでは部品を解放する。

## 検証

`--ocr-rendering-test <新しい出力フォルダー>`で、合成PDFを使った実画面の検査、文字セルの無効化、ドラッグイベントとUndo、選択・ロック・検索の色、25／100／400%での境界線、Unicode結合文字・ZWJ絵文字、旧方式との画像比較を実行する。`metrics.json`、`checks.txt`、比較PNGを出力する。

性能比較は250領域×80文字（20,000文字）の初回部品作成・配置・描画を対象とする。比較用の旧方式は従来と同じ部品を使い、バインドやトリガーの負荷は含めない。全PDFの読込やアプリ全体の速度を示す値ではない。時間と割当量は実行環境に依存するため記録値とし、再発防止には「文字描画部分の部品数が文字数に比例しないこと」と「未変更セルの反復読込が再生成しないこと」を検査する。割当量は累積の管理メモリ割当であり、常駐メモリやピーク使用量ではない。

最終測定値と配布先は[実装状況](IMPLEMENTATION_STATUS.md)と[検証記録](outputs/PdfCorrectorium-Documentation/docs/11_Test/11-01_TestStrategy.md)へ記録する。

## 残る改善候補

現行ページの領域コンテナーは依然として全件生成する。スクロール範囲に応じた領域の生成・解放、複数ページプレビューへの対応、回転・文字幅ハンドルのさらなる遅延生成は別途検討する。PDF全体を保持するページ構成履歴、ポータブル自動保存、サムネイル全件保持、出力時の全ファイル読込は今回の変更範囲に含まない。

画面配置・操作仕様・プロジェクト形式は変わらないため、画面模式図の改訂は不要。旧設計PDFの再生成も行っていない。
