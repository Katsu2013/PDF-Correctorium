# 設定の持ち運びとワークスペースプリセット（dev.128）

「編集 → 設定」の **管理** タブで操作します。従来の「保存先」は、このタブ下部の折りたたみ項目へ移動しました。
共通のタブ・ドロップダウン・ボタンのデザインを使用します。PDFを開いていない状態でも設定できます。

dev.128では管理タブ先頭に「最近開いたファイル」の表示件数と履歴クリアも追加しました。[最近開いたファイルのガイド](RECENT-FILES.md)を参照してください。件数は設定の書き出しに含まれますが、ファイル履歴そのものは含みません。

## 設定を別の環境へ持ち運ぶ

1. 「管理 → 書き出し」で、設定画面の現在の内容をJSONファイルへ保存します。画面で編集途中の値も対象です。
2. 別の環境で「管理 → 取り込み」を選び、この機能で書き出したJSONを指定します。
3. 検証後の確認に同意すると、設定画面の下書きとプリセット一覧を取り込んだ内容で置き換えます。追加マージではありません。
4. 内容を確認して「保存」を押すと、アプリへ反映され、次回起動時にも保持されます。「キャンセル」で閉じれば取り込み前のアプリ設定を維持します。

書き出しだけでは本画面へ設定を反映しません。一方、書き出した外部ファイルは、後で設定画面をキャンセルしても残ります。

対象は表示・編集補助・自動保存・バックアップ世代数・8種類の編集ショートカット・登録したプリセットです。PDF、プロジェクト、文書の編集内容、保存先パス、ログ、認証情報は含みません。ショートカットは保存後に既存のツールチップへ反映されます。

取り込みは最大1 MiB。識別子・版・JSON構造を確認し、未来の版、不正または重複・予約済みのショートカット、不正なプリセット名、重複したJSON項目等を拒否します。数値範囲や色は既存設定と同じ規則で正規化します。失敗時には設定画面の下書きもアプリの設定ファイルも変更しません。アプリ内部の生の `config/settings.json` は持ち運び形式と異なるため、取り込み対象ではありません。現在使用中の設定ファイルへ「書き出し」で上書きすることも禁止します。

## 配置を名前付きで保存する

1. 本画面のスプリッター、または設定の「表示」タブで、パネル幅・表示状態を調整します。
2. 「管理」で名前を入力し、「登録・更新」を押します。同じ名前があれば更新確認を表示します。
3. 「保存」でプリセットの登録を確定します。
4. 切り替えるときはプリセットを選んで「選択を適用」。必要なら「表示」タブで調整し、「保存」で本画面へ反映します。

保存するのは次の6項目です。

- ページ一覧パネルの表示、幅（160～420 DIP）
- OCRプロパティパネルの表示、幅（240～600 DIP）
- ステータスバーの表示
- ページサムネイルの表示

最大20件、名前は前後空白を除き1～64文字です。英字の大小は同名として扱い、制御文字は許可しません。「削除」は確認後に下書きから削除し、「保存」で確定します。「既定値に戻す」は登録済みプリセットを保持してその他の設定を初期化します。

プリセットの適用は言語、ショートカット、自動保存設定などの編集中の値を変更しません。PDFの編集内容、Undo履歴、選択中の編集モード、倍率、開いている文書は保存対象ではありません。手動倍率は維持しますが、「幅に合わせる」等の自動フィット中は、パネル幅に応じて表示倍率が再計算されます。ドッキング・フロート機能や、ショートカット専用の名前付きプリセットではありません。

## キー操作

| 操作 | キー | 由来 |
|---|---|---|
| 管理タブ | Alt+G | ManaGe（他タブの入力と競合しない文字） |
| 最近開いたファイルの表示件数／履歴クリア | Alt+C／Alt+H | Count／History |
| 取り込み／書き出し | Alt+I／Alt+X | Import／eXport |
| プリセット／名前 | Alt+P／Alt+N | Preset／Name |
| 登録・更新／選択を適用／削除 | Alt+U／Alt+A／Alt+L | Update／Apply／deLete |
| 保存先の展開 | Alt+F | Folder |
| 設定の保存 | Alt+S | Save |

Tab／Shift+Tabで、表示件数 → 履歴クリア → 取り込み → 書き出し → プリセット → 名前 → 登録・更新 → 選択を適用 → 削除 → 保存先 → 下部操作へ移動します。無効なボタンや閉じたタブ内の操作は飛ばします。キャンセルには新しいアクセスキーを付けず、Escを使用します。

## 保存形式

アプリ設定の `formatVersion` は13です。`workspacePresets` がない従来の設定は空の一覧として、`recentFileLimit` がない設定は10件として読み込みます。ファイル履歴は別ファイルに保存します。プロジェクト形式は引き続き1.1で、変更していません。

持ち運び用JSONは `format: "PdfCorrectoriumSettings"`、持ち運び形式の `formatVersion: 1`、書き出し時の `applicationVersion`、設定本体の `settings` を持ちます。アプリ設定の形式番号と持ち運び形式の番号は別です。

設定ファイルと書き出し先は、同じフォルダーの一意な一時ファイルを完成させてから置き換えます。書き込み失敗・中止では既存ファイルを維持します。
