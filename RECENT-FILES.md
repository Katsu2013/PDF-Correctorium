# 最近開いたファイル（dev.128）

## 一覧から開く

「ファイル → 最近開いたファイル」を開き、PDFまたはプロジェクトを選択します。成功した読み込みを新しい順に表示し、同じファイルを開き直すと先頭へ移動します。ファイル名とフォルダーを表示し、長い表示は省略しますが、ツールチップに完全なパスを表示します。ファイル名内の `_` はアクセスキーとして解釈しません。

通常のファイル選択と、ファイル関連付け・起動引数からの成功した読み込みも履歴へ記録します。プロジェクトの場合は元PDFではなく `.pdfocrproj` 自体を記録します。PDF出力やプロジェクト保存だけでは追加しません。

PDFを開く前でも、履歴があれば利用できます。履歴なし・表示件数0・文書読み込みや保存／出力などの処理中は無効です。編集中の文書があれば、通常の読み込みと同じ保存・破棄・キャンセル確認を行います。読み込み失敗時には現在の文書、編集、Undoを維持します。

移動・削除されたファイルはエラーを表示します。取り外し可能なドライブ等を考慮し、失敗した項目は自動削除しません。ファイルの新しい場所を通常の「開く」で指定するか、不要な履歴をクリアしてください。履歴の表示だけでは各文書を読み込みません。

## 表示件数を変更する

「編集 → 設定 → 管理 → 表示件数」を変更して「保存」します。既定は10件、0～30の整数を指定できます。

- 1～30件：指定数まで表示し、成功した読み込みを記録します。
- 0件：一覧の表示と新しい履歴の記録を停止します。保存済みの履歴を削除する設定ではありません。
- 内部では最大30件を保持します。表示件数を減らしても非表示になった項目は残り、再び増やすと表示できます。31件目以降の古い項目は保持しません。

## 履歴を消す

「管理 → 履歴をクリア」で確認に同意し、設定画面の「保存」を押します。「キャンセル」で閉じた場合や設定の保存に失敗した場合は、履歴を消しません。履歴ファイル自体の書き込みに失敗した場合はエラーを表示し、クリア成功と扱いません（件数など、先に保存した設定は反映済みです）。

消すのはこのアプリの履歴一覧だけです。PDF・プロジェクト・バックアップは削除せず、開いている文書も閉じません。履歴が空ならクリアボタンを無効にします。個別項目の削除・ピン留めは提供していません。

## 保存とプライバシー

設定フォルダーの `recent-files.json` に絶対パスを保存し、再起動後も復元します。Portable版は実行フォルダー内の `config`、Installed版はアプリ用AppData設定フォルダーです。保存先を共有する複数の起動ウィンドウ間で更新を調整し、ファイルメニューを開くと最新の履歴を再読込します。別のPortableフォルダーとは履歴を共有しません。

表示件数 `recentFileLimit` はアプリ設定（形式13）に含めます。古い設定は10件として読み込みます。履歴は別形式（形式1）で、プロジェクトや設定のJSON書き出しには含めません。設定の取り込みで履歴は置き換えず、配置プリセットも履歴・件数を変更しません。「既定値に戻す」は件数を10に戻すだけで、履歴を消しません。

履歴は暗号化されません。Portableフォルダー全体を他の人へ渡す場合は、先に履歴をクリアしてください。この操作はログやWindows側の履歴を消すものではありません。記録も止めたい場合は表示件数を0にします。

## キー操作

ファイルメニューは Alt+F → R（Recent）、一覧は矢印キーとEnterで操作します。設定の管理タブはAlt+G、表示件数はAlt+C（Count）、履歴クリアはAlt+H（History）、保存はAlt+Sです。表示件数からTabで履歴クリアへ移動します。OK・キャンセルへのアクセスキー追加は行っていません。

自動検証は `--recent-files-test <新しい出力フォルダー>` で実行します。対象・結果・操作試験の限界は[テスト戦略](outputs/PdfCorrectorium-Documentation/docs/11_Test/11-01_TestStrategy.md)を参照してください。
